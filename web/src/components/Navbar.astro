---
import { SignedIn, SignedOut, UserButton, SignInButton, SignUpButton } from "@clerk/astro/components";

interface Props {
  pageTitle: string;
}

const { pageTitle } = Astro.props;
---

<nav class="sticky top-0 z-50 backdrop-blur-md bg-base-100/80 border-b border-base-300">
  <div class="navbar container mx-auto">
    <div class="navbar-start">
      <a href="/" class="btn btn-ghost text-2xl font-bold text-primary">R</a>
    </div>
    
    <div class="navbar-center">
      <span class="text-base-content font-medium">{pageTitle}</span>
    </div>
    
    <div class="navbar-end">
      <SignedOut>
        <a href="/iniciar-sesion" class="btn btn-primary btn-sm">Iniciar sesión</a>
        <a href="/registrarse" class="btn btn-secondary btn-sm ml-2">Registrarse</a>
      </SignedOut>
      <SignedIn>
        <UserButton />
      </SignedIn>
    </div>
  </div>
</nav>
